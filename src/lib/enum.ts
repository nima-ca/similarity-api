export enum colorModes {
  SYSTEM = "system",
  DARK = "dark",
  LIGHT = "light",
}

export enum SupportedLanguages {
  PYTHON = "python",
  NODEJS = "nodejs",
}

export enum methods {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  PATCH = "PATCH",
  DELETE = "DELETE",
  OPTIONS = "OPTIONS",
}

export enum loginProviders {
  Google = "google",
  Github = "github",
}
